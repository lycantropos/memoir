version: '3'

services:
  memoir-cpython:
    build:
      context: .
      args:
      - PYTHON_IMAGE=${CPYTHON_IMAGE_NAME}
      - PYTHON_IMAGE_VERSION=${CPYTHON_IMAGE_VERSION}
    image: lycantropos/memoir-cpython:0.0.1
    volumes:
      - ./memoir/:/opt/memoir/memoir/
      - ./tests/:/opt/memoir/tests/
      - ./README.md:/opt/memoir/README.md
      - ./requirements-tests.txt:/opt/paradigm/requirements-tests.txt
      - ./setup.py:/opt/memoir/setup.py
      - ./setup.cfg:/opt/memoir/setup.cfg
    entrypoint: pytest
